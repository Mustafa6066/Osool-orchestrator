# Railway Configuration for @osool/admin (Vite SPA served via nginx)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.admin"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
